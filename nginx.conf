server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 헬스체크용 (ALB 상태 검사용)
    location /health {
        return 200 'healthy';
        add_header Content-Type text/plain;
    }
}